#include<iostream>
#include<vector>
#include<string>
#include<algorithm>

using namespace std;
void input(vector<int>& v);
void input2(vector<int>& v, int a, int b);
void print(vector<int>& v);
void middle(vector<int>& v1, vector<int>& v2, vector<int>& v3);
void erased(vector<int>& v1, vector<int>::iterator it);
void rev_sort(vector<string>& v);